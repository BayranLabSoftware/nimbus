node_modules/
dist/
build/
coverage/
playwright-report/
test-results/
storybook-static/
.pnpm-store/
.cache/
.vite/
pnpm-lock.yaml
package-lock.json
CHANGELOG.md
# Auto-generated by `pnpm validation-report`; the generator is the
# single source of truth for the layout. Prettier and JSON.stringify
# disagree on single-element arrays, so checking these creates a
# false positive on every CI run.
docs/VALIDATION_REPORT.md
docs/VALIDATION_REPORT.json
public/luts/
public/hdri/
public/models/
public/data/
# Written by the benchmark campaign's scripts (scripts/benchmark/), which
# are the source of truth for their layout, as for the validation report.
benchmark/nimbus/
benchmark/results/
benchmark/dart/
docs/BENCHMARK_REPORT.md

# The seal of the impacts module: written by `scripts/seal-impacts.ts`, one
# line per reading so a moved digest is one line of the diff. Prettier would
# pretty-print it back to half a megabyte and rewrite the file on every
# commit — which is a re-seal nobody asked for (rule 833).
src/seal/impactSealData.json
