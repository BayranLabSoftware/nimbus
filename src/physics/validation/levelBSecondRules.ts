/**
 * Rules 875 to 880 — the second round of level B, its sets frozen before the
 * physics it will test is written. Written on 23 September 2026, on Andrea's
 * word («vai»), after the first round was not earned (d130dfa) and the
 * reviewer asked that the next list be "completely new, frozen before the
 * change to the physics of strength". No line of that physics exists yet.
 *
 * RULE 875. WHAT THIS IS. The second round of level B: the model phase 4 will
 * produce, held to observed events chosen now, so that nothing of the new
 * physics can be shaped on them. Its sources are not opened until its own step
 * 2, after the phase-4 model is frozen. Its method is the first round's —
 * rules 855 to 874: four steps, the conversions of rule 866, the bars of rule
 * 868, the family rule of 870 — unless its own rules, written before its step
 * 2, say otherwise. Following the reviewer (23 September), its E2, the
 * altitude of the first major fragmentation, is read as a test of the first
 * stage of a body's strength, and its E3, the flare, as a joint test of the
 * second stage, the cascade of fragments and the light they give — never of
 * the strength alone.
 *
 * RULE 876. THE SETS.
 *   (a) Entry, counted: 2022 WJ1 (19 November 2022, over Ontario) — seen by
 *       telescopes before it entered, in no file of the repository, with an
 *       analysis from telescope to fireball (arXiv:2411.14595).
 *   (b) Entry, conditional — counted only if, at step 2, a pinned
 *       peer-reviewed source reports an altitude of fragmentation or of a
 *       flare that was observed, not computed from an assumed strength:
 *       2024 XA1 (3 December 2024, Yakutia), 2024 UQ (22 October 2024, over
 *       the Pacific), 2026 RW1 (6 September 2026, over the Indian Ocean).
 *   (c) Crater, counted: Sterlitamak (17 May 1990), an iron that fell before
 *       witnesses and dug a single crater — the only eyewitnessed
 *       crater-forming fall besides Carancas and Sikhote-Alin, and the
 *       reviewer's "second independent positive case, preferably an iron".
 *       Counted only if its body's mass can be pinned from the recovered
 *       meteorite and the geophysical search of the crater, not from the
 *       crater's size (rule 860(c)).
 *   (d) Excluded, and why: 2014 AA (recorded by infrasound only: no
 *       altitude); 2026 JN4 (its impact is unconfirmed); 2019 MO and
 *       2024 RW1 (rows of I2's CNEOS set); 2008 TC3, 2018 LA and 2022 EB5
 *       (seen); 2023 CX1, 2024 BX1 and Carancas (the first round's, now
 *       diagnostic); Chelyabinsk, Tunguska and Sikhote-Alin (development);
 *       and every fall of Borovička, Spurný & Shrbený (2020), from whose
 *       sample phase 4 is to draw its priors of strength.
 *   (e) With fewer than two counted bodies of the entry the round claims no
 *       B of the entry: it reports "compatible in one case", as rule 870
 *       words a single crater.
 *
 * RULE 877. WHAT PHASE 4 MAY LEARN FROM. Its physics of strength may be drawn
 * only from the development set, the seen set, and published statistics of
 * falls outside the sets of rule 876 (Borovička et al. 2020 and the like),
 * each use written in the rules that make it. 2023 CX1, 2024 BX1 and Carancas
 * are regression cases: run and reported, never fitted to. Nothing of rule
 * 876's events may be read while phase 4 is written.
 *
 * RULE 878. WHAT WAS ALREADY SEEN of the new sets, declared: the search
 * results that found them quoted, for 2022 WJ1, the dynamic mass of its
 * surviving main fragment and the dynamic pressures of its fragmentation
 * (none of note below 0.3 MPa, most of its mass lost above 0.8 MPa); for
 * 2024 XA1, altitudes of its main fragmentation computed for assumed
 * strengths of 0.5, 1 and 5 MPa; for 2026 RW1, a size of about 0.8 m; for
 * Sterlitamak, a crater about 10 m wide and 4 to 5 m deep with a rim of 60 to
 * 70 cm, a 315 kg piece recovered from it, and a body estimated above a ton.
 * No source of them has been opened. 2022 WJ1's pressures bear on the very
 * quantity phase 4 will model, and are written here so that the priors of
 * phase 4 can be seen to come from elsewhere (rule 877).
 *
 * RULE 879. WHAT MAY NOT HAPPEN. After this commit no event is added to the
 * sets of rule 876 or removed from them; the conditional ones are decided
 * only by their own clause, at step 2. The sets do not wait for the physics:
 * if phase 4 runs long, a body that impacts after today is not added to this
 * round.
 *
 * RULE 880. WHAT AN OUTCOME DOES. The same as the first round's (rules 864
 * and 870): a family earns a B limited to its domain, or names what failed.
 *
 * RULE 903. THE AUDIT, 23 September 2026, afternoon, as the reviewer asked
 * that day (every candidate absent from all the development data, not only
 * from I2's 357 fireballs). The whole repository was searched, by name and by
 * date, for 2022 WJ1 (2022-11-19), 2024 XA1 (2024-12-03), 2024 UQ
 * (2024-10-22), 2026 RW1 (2026-09-06) and Sterlitamak: none is in the CNEOS
 * set or any fireball file, in any test, preset, fixture, benchmark result,
 * threshold table or script. Each name appears only in the ROADMAP's lines
 * that froze this round; "Sterlitamak" also as a town in the list of cities
 * the globe labels, which holds no datum of the fall. None is in the sample
 * of Borovička, Spurný & Shrbený (2020) — falls and fireballs of 2009 to
 * 2018 — from which the strength law of rules 896 to 902 took its priors.
 * The audit is repeated at step 2, before any source is pinned.
 *
 * RULE 904. WHAT STEP 2 OF THIS ROUND MUST PIN, as the reviewer asked (23
 * September), before the model is run on any of its events: for each
 * candidate, one row — its primary source and the date it was published; how
 * each altitude was measured, and whether it was measured or derived from a
 * model; its uncertainty; the definition of the observable; its presence or
 * absence in the CNEOS set and in the literature the priors came from. The
 * outcome E1 of 2022 WJ1 is "no crater" — not "no fragment reached the
 * ground", for radar followed fragments down to about a kilometre over Lake
 * Ontario. 2024 UQ's altitude, which ESA attributes to CNEOS, counts only if
 * it is an independent observation with its definition and uncertainty.
 * Sterlitamak's mass: its recovered 315 kg is a fragment, and an estimate of
 * the whole from the crater is circular (rule 860(c)); unless a constraint
 * independent of the crater is pinned, it stays diagnostic (rule 876(c)).
 */

/**
 * Rules 919 to 926 — step 2 of the second round, and the model it holds.
 * Written on 23 September 2026, afternoon, on Andrea's word («Prosegui il
 * lavoro»), after the reviewer closed the atmosphere's round and asked that
 * this round be run on the operative branch with no further change to the
 * model; written before any source of rule 876's events is opened.
 *
 * RULE 919. THE MODEL, FROZEN. Phase 4's model is frozen at 5b6b7a5:
 * `simulateImpact` with its defaults — the two-stage strength of rules 896 to
 * 902, Collins's closed forms on his exponential atmosphere, the Monte Carlo's
 * summaries of rules 890 to 895 and 905 to 907 — on Node 22.20.0 and
 * darwin-arm64. The predictions are computed from a checkout of that commit,
 * as rule 859 computed the first round's, whatever the repository holds by
 * then. A change to the model after it is tested by a later round.
 *
 * RULE 920. THE AUDIT, REPEATED as rule 903 says, before any source is opened
 * (23 September 2026, afternoon): the repository searched again by name and by
 * date for every event of rule 876. The same result: the names stand only in
 * the ROADMAP, in rules 875 to 880 and 903 to 904 and their test, and
 * "Sterlitamak" in the list of cities the globe labels; "WJ1" and "XA1" also
 * match bytes of three map tiles (PNG images of population and Vs30), which
 * hold no datum of any fall.
 *
 * RULE 921. THE OBSERVABLES OF THE ENTRY, as rule 875 reads them, against the
 * frozen model, with rule 868's bars:
 *   E1 the outcome, "no crater" (rule 904) — primary — passes if at least 90 %
 *      of the draws dig no crater of any kind (`crater.origin` is `none`);
 *   E2 the altitude of the first major fragmentation — primary — read as a
 *      test of the first stage of the body's strength: against
 *      `entry.firstFragmentationAltitude` where the two-stage law covers the
 *      body's density, against `entry.breakupAltitude` elsewhere;
 *   E3 the flare — secondary — against `entry.burstAltitude`, a joint test of
 *      the second stage, the cascade of fragments and the light they give.
 * Reported beside them, never scored: `entry.breakupAltitude` where E2 reads
 * the first stage, against the later fragmentations the sources report. An
 * altitude a source derives from an assumed strength or from a model of the
 * entry, not measured from light curves, video, radar or infrasound, is no
 * target (rule 904); it is written down as such.
 *
 * RULE 922. THE CRATER. Sterlitamak counts only if rule 904's clause is met —
 * a mass of its body pinned from a constraint independent of the crater; else
 * it is run and reported as a diagnostic case, never scored: K1 (a crater dug
 * by a body that reached the ground), K3 (depth over diameter) and K4 (the
 * morphology) beside its observed values. As in the first round no strength
 * is an input: the model's own law gives an iron its strength.
 *
 * RULE 923. SAMPLING. Rule 863(a) with the seed `level-b2-2026-09-23` and
 * 1 000 draws per event; an angle a source gives only as representative or
 * best-fitting is drawn as rule 869 draws Carancas's, uniformly over the range
 * the source allows, and the range is pinned with its page.
 *
 * RULE 924. THE STANDARD ATMOSPHERE AS A SENSITIVITY, as the reviewer allowed:
 * the same draws, from the same frozen commit, run again with
 * `entryAtmosphere: 'integratedUssa'` and reported beside the operative
 * branch's — never scored, never used to earn or refuse a class.
 *
 * RULE 925. WHAT STEP 2 COMMITS, before any prediction: the sources of every
 * candidate in the rows rule 904 asks for; every input with its value, its
 * interval and its page; every target with its source and page and no value,
 * as the first round pinned them; each conditional event decided by rule
 * 876(b)'s clause, with its reason; and the domain a B would claim, stated
 * from the counted bodies' inputs as rule 871 stated the first round's. Every
 * download is asked of Andrea first, with its name, source and size. The
 * summary goes to the reviewer before step 3, as rule 855 sent the first.
 *
 * RULE 926. WHAT DECIDES: rule 870 as written, on this round's counted events
 * alone — the first round's are regression cases, run and reported (rule
 * 877). With fewer than two counted bodies of the entry the round claims no B
 * of the entry and reports "compatible in one case" (rule 876(e)); the crater,
 * if Sterlitamak counts, earns at most "compatible with Sterlitamak in one
 * reference case", and with Carancas's failure standing, "validated" stays
 * out of reach.
 */

/**
 * Rules 927 to 932 — the reviewer's corrections to step 2, adopted before
 * step 3 as rule 865 allows. Written on 23 September 2026, afternoon, from his
 * reply of that day to the summary of step 2 (sent by Andrea as «Nimbus —
 * secondo giro B, fonti per il revisore»); each is his. No prediction has
 * been computed.
 *
 * RULE 927. THE ROUND GOES ON, AS A PRE-REGISTERED DIAGNOSTIC AUDIT. With one
 * counted body it can earn no class B; it tests whether the two-stage model
 * avoids a gross error on a sub-metre body of its declared domain. Its words
 * are fixed now: if 2022 WJ1 passes, "compatible with 2022 WJ1 in one
 * independent case"; if not, "incompatible with 2022 WJ1; no class B"; and
 * either way, "not a validation of a population nor an estimate of coverage".
 *
 * RULE 928. E2 STAYS OUT, and is reported so. The heights of the source's
 * fragmentation table come from a model fitted to the light curve and not
 * unique: testing the model against them would compare two models. The
 * absence of a first-stage fragmentation is no pass-or-fail target either —
 * one event, and a model that gives a major first phase in two draws in three.
 * It is reported as a qualitative check, unscored, in the reviewer's words: "No
 * identifiable initial fragmentation was observed at dynamic pressures up to
 * 0.1 MPa; the comparison is not assessed quantitatively, because an absence
 * does not determine a unique observed altitude."
 *
 * RULE 929. E3 IS "COMPATIBILITY WITH THE FLARE HEIGHTS", never "the altitude
 * of the main fragmentation": a joint test of the strength's threshold, the
 * cascade of fragments, the ablation and the light.
 *
 * RULE 930. E1 AND E3 HOLD TOGETHER. E1, "no crater", passes if at least 90 %
 * of the draws dig no crater. E3 is read only on the draws that satisfy E1:
 * its 5–95 % band is theirs, and it is compatible only if that band meets the
 * observed heights widened by 5 km (rule 868). A model that meets the flares
 * only in draws that dig a crater or reach the ground is not compatible with
 * the event. The share of the E1 draws is reported beside.
 *
 * RULE 931. STERLITAMAK, diagnostic. Its 30° to 60° is reported as an
 * analytical choice, not an interval observed. Its target is run at 1 800,
 * 2 200 and 2 700 kg/m³ with no probability given to any, to show whether
 * "crater, crater field or no crater" depends on the ground assumed; 2 700 is
 * the reference grid's rock, not a stand-in for plastic clay and black earth.
 *
 * RULE 932. THE THIRD SET, kept for the round on the dynamics of
 * fragmentation, must hold before any new request for level B: at least three
 * new bodies with their altitude measured directly, and at least one that
 * reached the ground or dug a crater with inputs not fitted to it.
 */

/** Rule 931: Sterlitamak's grounds (kg/m³). */
export const LEVEL_B2_STERLITAMAK_GROUNDS = [1_800, 2_200, 2_700] as const;

/** Rule 919: the model under test. */
export const LEVEL_B2_FROZEN_MODEL = '5b6b7a5';

/** Rule 923. */
export const LEVEL_B2_SEED = 'level-b2-2026-09-23';
export const LEVEL_B2_DRAWS = 1_000;

export type LevelB2Set = 'entry' | 'entry-conditional' | 'crater' | 'excluded';

/** Rule 876. */
export const LEVEL_B2_EVENTS: Readonly<Record<string, LevelB2Set>> = {
  '2022 WJ1': 'entry',
  '2024 XA1': 'entry-conditional',
  '2024 UQ': 'entry-conditional',
  '2026 RW1': 'entry-conditional',
  Sterlitamak: 'crater',
  '2014 AA': 'excluded',
  '2026 JN4': 'excluded',
  '2019 MO': 'excluded',
  '2024 RW1': 'excluded',
};

/** Rule 876(e): the counted bodies of the entry a B needs. */
export const LEVEL_B2_MIN_ENTRY_BODIES = 2;

/**
 * The outcome, written on 23 September 2026 after step 4, from the predictions
 * committed in aaec9bf and the observed values of levelB2Targets.ts, scored by
 * levelB2Score.ts (recomputed by its test).
 *
 * INCOMPATIBLE WITH 2022 WJ1; NO CLASS B — not a validation of a population
 * nor an estimate of coverage (rule 927).
 *
 * 2022 WJ1, counted. E1 FAILS: 8.2 % of the draws dig no crater, against a bar
 * of 90 %. In the other 91.8 % the model keeps the body whole — its dynamic
 * pressure never reaches the second stage's strength, 2.12 MPa at the prior's
 * midpoint — and brings it to the ground at about 0.1 km/s, where it still
 * digs a crater of 2.3 to 3.4 m. The body broke up in the air; its largest
 * fragment, of some 9 to 20 kg, fell without digging one. E3, read on the 82
 * draws that dig no crater (rule 930): the model deposits its energy at 28.7
 * to 30.8 km (median 29.8), against flares at 34.0 to 38.7 km widened by 5 km
 * — it meets them, by 1.8 km, and a secondary target cannot carry a failed
 * primary. E2, qualitative (rule 928): the model places the first stage at
 * 63.4 to 63.5 km, a major first phase in two draws in three; "No
 * identifiable initial fragmentation was observed at dynamic pressures up to
 * 0.1 MPa; the comparison is not assessed quantitatively, because an absence
 * does not determine a unique observed altitude."
 *
 * The standard atmosphere, a sensitivity (rule 924): on the same draws 43.4 %
 * dig no crater — E1 fails as well; E3 meets.
 *
 * Sterlitamak, diagnostic (rules 922 and 931): its mass of 1.4 to 2.6 t is
 * fitted to the crater, its 30° to 60° an analytical choice. On every ground —
 * 1 800, 2 200 and 2 700 kg/m³ — every draw digs a single simple crater, so
 * that outcome does not hang on the ground assumed; its median diameter is
 * 20.5, 19.2 and 17.9 m against the 9.4 to 10 m observed, and its depth over
 * diameter 0.21 against 0.32 to 0.50.
 *
 * The first round's bodies on the new model (rule 877), reported: 2024 BX1
 * bursts in the air (E1) and now meets its flares (E3, 34.8–35.0 km against
 * 33.9–35.2), where the first round missed them; its first stage at 64.9 km
 * misses the 55 km where its fragmentation began (E2). 2023 CX1 bursts in the
 * air, its first stage at 63.6 km and its energy at 33.4–33.7 km against a
 * first fragmentation at 29.4 km and flares at 27–28 km: E2 and E3 miss.
 * Carancas digs no crater in any draw, as before.
 *
 * What it names for phase 4: the second stage is one strength for the whole
 * body, so a stone whose dynamic pressure peaks near it either stays whole or
 * breaks at once — 2022 WJ1 fragmented step by step from about 0.4 MPa; a
 * body that reaches the ground at terminal speed is given a hypervelocity
 * crater; and the first stage sits at 61 to 66 km for every body, where
 * 2024 BX1's began at 55 km and 2022 WJ1 showed none. The third set of rule
 * 932 is what a later round is judged on.
 */
export const LEVEL_B2_OUTCOME: string | null =
  'Incompatible with 2022 WJ1; no class B — 23 September 2026. Not a validation of a population nor an estimate of coverage. The model keeps the body whole in 92 % of its draws and digs a crater where it broke up in the air; the flares are met only on the draws that burst. Sterlitamak, diagnostic: a single simple crater on every ground, twice as wide as observed.';
